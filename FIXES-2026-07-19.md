# Imsfrane site — corrected build (19 July 2026)

Main corrections:
- Fixed every missing Quad image reference; the Quad page now uses four valid WebP photos.
- Added responsive WebP variants and srcset/sizes to reduce mobile downloads.
- Replaced the oversized logo asset in page markup with a 104 px version.
- Split the very large global CSS and JavaScript into compact common/page bundles.
- Loaded Google Fonts and Font Awesome without blocking the first render.
- Fixed the closed mobile navigation using inert instead of aria-hidden around focusable controls.
- Improved low-contrast labels and WhatsApp buttons.
- Enlarged slider touch targets to 44 × 44 px while keeping the visual dots small.
- Added clear focus-visible styling and reduced-motion support.
- Added unique accessible names to activity WhatsApp links.
- Throttled the scroll handler to reduce forced layout work.

Important:
- GitHub Pages controls the 10-minute cache header. A static-code change cannot increase that server TTL.
- Editable source files remain in css/style.css and js/script.js. Delivery pages use css/core.min.css, css/pages/*.min.css, js/core.min.js and js/pages/*.min.js.
